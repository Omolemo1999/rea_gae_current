"use client";
import { Alert, Box, Button, CircularProgress, Stack, TextField, Typography } from "@mui/material";
import MailOutlineRoundedIcon from "@mui/icons-material/MailOutlineRounded";
import Link from "next/link";
import { useState } from "react";
import AuthCard from "@/components/auth/AuthCard";
export default function ForgotPasswordPage(){const [email,setEmail]=useState("");const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setMessage("");const r=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});const d=await r.json();setBusy(false);setMessage(d.message||"If the account exists, a reset link has been sent.")};return <Box sx={{minHeight:"100vh",display:"grid",placeItems:"center",p:2,background:"radial-gradient(circle at 80% 10%,rgba(91,108,255,.16),transparent 35%),#F6F8FC"}}><AuthCard title="Forgot your password?" subtitle="No stress. Enter your email and we'll send you a secure link to create a new password."><Stack alignItems="center" sx={{mb:3}}><Box sx={{width:58,height:58,borderRadius:4,display:"grid",placeItems:"center",bgcolor:"rgba(91,108,255,.1)",color:"primary.main"}}><MailOutlineRoundedIcon/></Box></Stack>{message&&<Alert severity="success" sx={{mb:2}}>{message}</Alert>}<Box component="form" onSubmit={submit}><TextField fullWidth label="Email address" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" required/><Button fullWidth size="large" variant="contained" type="submit" disabled={busy} sx={{mt:2}}>{busy?<><CircularProgress size={20} sx={{mr:1}}/>Sending secure link…</>:"Send reset link"}</Button></Box><Typography variant="caption" color="text.secondary" sx={{display:"block",mt:2,textAlign:"center"}}>For security, we use the same message whether or not the email belongs to a ReaGae account.</Typography><Button component={Link} href="/login" sx={{mt:2}}>Back to login</Button></AuthCard></Box>}
