"use client";
import { Alert, Avatar, Box, Button, Chip, Paper, Stack, Typography, MenuItem, TextField } from "@mui/material"; import { useParams,useRouter } from "next/navigation"; import { useEffect,useState } from "react"; import AuthGuard from "@/components/AuthGuard"; import DashboardShell from "@/components/dashboard/DashboardShell"; import Mascot from "@/components/common/Mascot"; import type { Ride } from "@/types/ride"; import LiveMap from "@/components/maps/LiveMap"; import {calculateFees} from "@/lib/pricing";
export default function RiderRideDetails(){const {id}=useParams<{id:string}>();const router=useRouter();const [ride,setRide]=useState<any>(null);const [livePoint,setLivePoint]=useState<any>(null);const [error,setError]=useState("");const [seats,setSeats]=useState(1);const [bags,setBags]=useState(0);const [busy,setBusy]=useState(false);const [collection,setCollection]=useState({name:"",type:"MALL",latitude:"",longitude:""});const [verification,setVerification]=useState<any>(null);useEffect(()=>{fetch(`/api/rides/${id}`).then(r=>r.json()).then(d=>setRide(d.ride||null));fetch("/api/verification/rider").then(r=>r.json()).then(d=>setVerification(d.profile))},[id]);useEffect(()=>{if(!ride)return;const poll=async()=>{const r=await fetch(`/api/tracking/${id}`);if(r.ok){const d=await r.json();if(d.location)setLivePoint({latitude:d.location.latitude,longitude:d.location.longitude})}};poll();const timer=window.setInterval(poll,5000);return()=>window.clearInterval(timer)},[id,ride?.id]);
const book=async()=>{if(verification?.verificationStatus!=="VERIFIED"){router.push("/rider/verification");return}setBusy(true);setError("");if(!collection.name.trim()){setError("Choose a public collection point.");setBusy(false);return}const r=await fetch("/api/payments/initialize",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({rideId:id,seatsRequested:seats,bags,collectionSpot:{...collection,latitude:Number(collection.latitude),longitude:Number(collection.longitude)}})});const d=await r.json();setBusy(false);if(!r.ok){setError(d.error||"Payment could not be started.");return}window.location.href=d.authorization_url};;
return <AuthGuard role="RIDER"><DashboardShell role="RIDER">{ride?<><Stack direction={{xs:'column',sm:'row'}} justifyContent="space-between" gap={2} alignItems={{sm:'center'}}><Box><Typography variant="h3">{ride.pickupName} → {ride.destinationName}</Typography><Typography color="text.secondary" sx={{mt:1}}>{ride.departureDate} · {ride.departureTime}</Typography></Box><Mascot compact/></Stack><Stack direction="row" gap={1} sx={{mt:3}} flexWrap="wrap"><Chip label={`${ride.distanceKm} km`} /><Chip label={`${ride.availableSeats} seats available`} /><Chip label={`R${Number(ride.pricePerSeat).toFixed(0)} / seat`} color="primary" /></Stack>{verification?.verificationStatus!=="VERIFIED"&&<Alert severity="warning" sx={{mt:2}}>Before you book, ReaGae needs your ID and live face verification. <Button size="small" onClick={()=>router.push("/rider/verification")}>Verify now</Button></Alert>}<Box sx={{display:'grid',gridTemplateColumns:{xs:'1fr',md:'1.35fr .8fr'},gap:3,mt:3}}><Paper sx={{p:3,border:'1px solid',borderColor:'divider'}} elevation={0}><Typography variant="h6">Your journey</Typography><Box sx={{mt:3,p:2,borderRadius:3,bgcolor:'rgba(91,108,255,.07)'}}><Typography fontWeight={800}>Pickup</Typography><Typography color="text.secondary">{ride.pickupName}</Typography><Typography sx={{my:2,color:'primary.main',fontWeight:900}}>↓</Typography><Typography fontWeight={800}>Destination</Typography><Typography color="text.secondary">{ride.destinationName}</Typography></Box><LiveMap point={livePoint||undefined} pickup={ride?.pickup} destination={ride?.destination} height={360}/><Stack direction="row" alignItems="center" gap={1.5} sx={{mt:3}}><Avatar src={ride.driver?.profilePhotoUrl||undefined}>{ride.driver?.firstName?.[0]}</Avatar><Box><Typography><b>{ride.driver?.firstName} {ride.driver?.lastName}</b></Typography><Typography variant="body2" color="text.secondary">{livePoint?"Live location is updating every few seconds. ":""}Rating {Number(ride.driver?.ratingAverage||0).toFixed(1)} / 5 · {ride.vehicle?.make} {ride.vehicle?.model}</Typography></Box></Stack>{ride.vehicle?.registrationNumber&&<Alert severity="success" sx={{mt:2}}>Booking accepted — vehicle plate: <b>{ride.vehicle.registrationNumber}</b>. Match this before you get in.</Alert>}</Paper><Paper sx={{p:3,border:'1px solid',borderColor:'divider'}} elevation={0}><Typography variant="h6">Request seats & collection</Typography><Stack gap={2} sx={{mt:2}}><TextField select label="Collection point type" value={collection.type} onChange={e=>setCollection({...collection,type:e.target.value})}>{[["MALL","Mall"],["SHOPPING_CENTER","Shopping centre"],["TRANSIT_HUB","Transit hub"],["PUBLIC_VENUE","Public venue"]].map(x=><MenuItem key={x[0]} value={x[0]}>{x[1]}</MenuItem>)}</TextField><TextField label="Collection point" placeholder="e.g. Mall of Africa – main entrance" value={collection.name} onChange={e=>setCollection({...collection,name:e.target.value})}/><Alert severity="info">Home addresses are not accepted. Use a mall, shopping centre, transit hub or other public venue.</Alert><Box><Typography variant="body2" fontWeight={750}>Seats</Typography><Stack direction="row" alignItems="center" gap={1} sx={{mt:1}}><Button variant="outlined" onClick={()=>setSeats(Math.max(1,seats-1))}>−</Button><Typography sx={{minWidth:40,textAlign:'center',fontWeight:900}}>{seats}</Typography><Button variant="outlined" onClick={()=>setSeats(Math.min(ride.availableSeats,seats+1))}>+</Button></Stack></Box><Box><Typography variant="body2" fontWeight={750}>Bags</Typography><Stack direction="row" alignItems="center" gap={1} sx={{mt:1}}><Button variant="outlined" onClick={()=>setBags(Math.max(0,bags-1))}>−</Button><Typography sx={{minWidth:40,textAlign:'center',fontWeight:900}}>{bags}</Typography><Button variant="outlined" onClick={()=>setBags(Math.min(ride.luggageCapacity,bags+1))}>+</Button></Stack></Box>{error&&<Alert severity="error">{error}</Alert>}<Box><Typography>Fare: R{(Number(ride.pricePerSeat)*seats).toFixed(2)}</Typography><Typography color="text.secondary">Service fee: R{calculateFees(Number(ride.pricePerSeat)*seats).customerFee.toFixed(2)}</Typography><Typography variant="h5" fontWeight={900}>Total: R{calculateFees(Number(ride.pricePerSeat)*seats).totalCharge.toFixed(2)}</Typography><Typography variant="caption" color="text.secondary">Cash payments are not accepted.</Typography></Box><Button size="large" variant="contained" disabled={busy||ride.availableSeats<1} onClick={book}>{busy?'Requesting…':'Request booking'}</Button></Stack></Paper></Box></>:<Typography sx={{py:8,textAlign:'center'}}>Ride not found.</Typography>}</DashboardShell></AuthGuard>}
