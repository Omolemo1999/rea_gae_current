"use client";import {Alert,Box,CircularProgress,Container,Typography} from '@mui/material';import {useSearchParams,useRouter} from 'next/navigation';import {useEffect,useState} from 'react';
export default function PaymentCallback(){const q=useSearchParams();const router=useRouter();const [error,setError]=useState('');useEffect(()=>{const ref=q.get('reference');if(!ref){setError('Missing payment reference.');return}fetch('/api/payments/verify',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({reference:ref})}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error||'Payment failed');router.replace('/rider/payments')}).catch(e=>setError(e.message))},[q,router]);return <Container sx={{py:10,textAlign:'center'}}><Box>{error?<Alert severity="error">{error}</Alert>:<><CircularProgress/><Typography sx={{mt:2}}>Confirming your secure payment…</Typography></>}</Box></Container>}
