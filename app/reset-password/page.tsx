"use client";
import { Alert, Box, Button, InputAdornment, TextField } from "@mui/material"; import LockResetRoundedIcon from "@mui/icons-material/LockResetRounded"; import ArrowBackRoundedIcon from "@mui/icons-material/ArrowBackRounded";
import Link from "next/link"; import { Suspense, useState } from "react"; import { useSearchParams,useRouter } from "next/navigation"; import AuthShell from "@/components/auth/AuthShell"; import AuthCard from "@/components/auth/AuthCard"; import LoadingButton from "@/components/loading/LoadingButton"; import CarLoader from "@/components/loading/CarLoader";
function ResetForm(){const params=useSearchParams();const router=useRouter();const token=params.get('token')||'';const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');if(!token)return setError('This reset link is missing its security token.');if(password!==confirm)return setError('The passwords do not match.');setBusy(true);try{const r=await fetch('/api/auth/reset-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to reset password.');router.replace('/login?reset=success')}catch(e){setError(e instanceof Error?e.message:'Unable to reset password.')}finally{setBusy(false)}};return <AuthShell eyebrow="ACCOUNT SECURITY"><AuthCard eyebrow="NEW PASSWORD" title="Create a new password" subtitle="Choose a strong password you do not use on other services.">{error&&<Alert severity="error" sx={{mb:2}}>{error}</Alert>}<Box component="form" onSubmit={submit}><TextField label="New password" type="password" value={password} onChange={e=>setPassword(e.target.value)} required helperText="Use the password requirements shown by ReaGae." InputProps={{startAdornment:<InputAdornment position="start"><LockResetRoundedIcon fontSize="small"/></InputAdornment>}}/><TextField label="Confirm password" type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} required sx={{mt:2}}/><LoadingButton fullWidth size="large" variant="contained" type="submit" loading={busy} sx={{mt:2,minHeight:52}}>Update password</LoadingButton></Box><Button component={Link} href="/login" startIcon={<ArrowBackRoundedIcon/>} sx={{mt:2}}>Back to sign in</Button></AuthCard></AuthShell>}
export default function ResetPasswordPage(){return <Suspense fallback={<CarLoader fullScreen label="Loading secure reset…"/>}><ResetForm/></Suspense>}
