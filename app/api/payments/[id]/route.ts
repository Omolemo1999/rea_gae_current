import {NextResponse} from 'next/server';import {db} from '@/lib/db';import {getCurrentUser} from '@/lib/auth';
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const u=await getCurrentUser();const {id}=await params;if(!u||u.role!=='RIDER')return NextResponse.json({error:'Forbidden'},{status:403});const method=await db.paymentMethod.findUnique({where:{id}});if(!method||method.userId!==u.id)return NextResponse.json({error:'Card not found.'},{status:404});await db.paymentMethod.update({where:{id},data:{active:false}});return NextResponse.json({message:'Saved card removed.'});}
