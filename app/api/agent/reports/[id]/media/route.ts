import {NextResponse} from 'next/server';import {db} from '@/lib/db';import {getCurrentUser} from '@/lib/auth';
export async function GET(_req:Request,{params}:{params:Promise<{id:string}>}){const u=await getCurrentUser();if(!u||!['AGENT','ADMIN'].includes(u.role))return NextResponse.json({error:'Forbidden'},{status:403});const {id}=await params;const report=await db.safetyReport.findUnique({where:{id}});if(!report)return NextResponse.json({error:'Report not found.'},{status:404});const media=await db.safetyMedia.findMany({where:{bookingId:String(report.bookingId)},select:{id:true,type:true,fileName:true,mimeType:true,sizeBytes:true,durationSeconds:true,data:true,uploadedById:true,createdAt:true}});return NextResponse.json({media});}
