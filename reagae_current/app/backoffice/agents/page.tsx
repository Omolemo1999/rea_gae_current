"use client";import {Alert,Button,Grid,Paper,Stack,TextField,Typography,Box} from '@mui/material';import {useEffect,useState} from 'react';import AuthGuard from '@/components/AuthGuard';import DashboardShell from '@/components/dashboard/DashboardShell';
export default function Agents(){const [rows,setRows]=useState<any[]>([]);const [form,setForm]=useState({firstName:'',lastName:'',email:'',phone:'',password:''});const [msg,setMsg]=useState('');const load=()=>fetch('/api/admin/agents').then(r=>r.json()).then(d=>setRows(d.agents||[]));useEffect(load,[]);const create=async()=>{const r=await fetch('/api/admin/agents',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();setMsg(r.ok?'Agent created.':d.error||'Unable to create agent');if(r.ok){setForm({firstName:'',lastName:'',email:'',phone:'',password:''});load()}};return <AuthGuard role="ADMIN"><DashboardShell role="ADMIN"><Typography variant="h3">Agent management</Typography><Typography color="text.secondary" sx={{mt:1}}>Create and manage trusted operations staff.</Typography>{msg&&<Alert sx={{mt:2}}>{msg}</Alert>}<Paper sx={{mt:3,p:3}} elevation={0}><Typography variant="h5">Add agent</Typography><Grid container spacing={2} sx={{mt:1}}>{Object.keys(form).map(k=><Grid key={k} size={{xs:12,sm:6}}><TextField label={k.replace(/([A-Z])/g,' $1')} type={k==='password'?'password':'text'} value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></Grid>)}<Grid size={{xs:12}}><Button variant="contained" onClick={create}>Create agent</Button></Grid></Grid></Paper><Stack gap={2} sx={{mt:3}}>{rows.map(a=><Paper key={a.id} sx={{p:2.5}} elevation={0}><Stack direction="row" justifyContent="space-between"><Box><Typography fontWeight={850}>{a.firstName} {a.lastName}</Typography><Typography color="text.secondary">{a.email} · {a.phone}</Typography></Box><Button size="small" color={a.accountStatus==='ACTIVE'?'error':'success'} onClick={async()=>{await fetch('/api/admin/agents',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:a.id,accountStatus:a.accountStatus==='ACTIVE'?'SUSPENDED':'ACTIVE'})});load()}}>{a.accountStatus==='ACTIVE'?'Suspend':'Activate'}</Button></Stack></Paper>)}</Stack></DashboardShell></AuthGuard>}
