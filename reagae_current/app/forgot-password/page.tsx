"use client";
import { Alert, Box, Button, InputAdornment, TextField, Typography } from "@mui/material";
import MailOutlineRoundedIcon from "@mui/icons-material/MailOutlineRounded"; import ArrowBackRoundedIcon from "@mui/icons-material/ArrowBackRounded";
import Link from "next/link"; import { useState } from "react"; import AuthShell from "@/components/auth/AuthShell"; import AuthCard from "@/components/auth/AuthCard"; import LoadingButton from "@/components/loading/LoadingButton";
export default function ForgotPasswordPage(){const [email,setEmail]=useState('');const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setMessage('');try{const r=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const d=await r.json();setMessage(d.message||'If the account exists, a reset link has been sent.')}finally{setBusy(false)}};return <AuthShell eyebrow="ACCOUNT RECOVERY"><AuthCard eyebrow="SECURE RECOVERY" title="Reset your password" subtitle="We’ll send a secure link to the email address associated with your account.">{message&&<Alert severity="success" sx={{mb:2}}>{message}</Alert>}<Box component="form" onSubmit={submit}><TextField label="Email address" type="email" value={email} onChange={e=>setEmail(e.target.value)} required InputProps={{startAdornment:<InputAdornment position="start"><MailOutlineRoundedIcon fontSize="small"/></InputAdornment>}}/><LoadingButton fullWidth size="large" variant="contained" type="submit" loading={busy} sx={{mt:2,minHeight:52}}>Send secure link</LoadingButton></Box><Typography variant="caption" color="text.secondary" sx={{display:'block',mt:2,lineHeight:1.6}}>For security, ReaGae uses the same response whether or not the email belongs to an account.</Typography><Button component={Link} href="/login" startIcon={<ArrowBackRoundedIcon/>} sx={{mt:2}}>Back to sign in</Button></AuthCard></AuthShell>}
